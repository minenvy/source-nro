1. Install .Net version 5.0, docker, docker-compose
2. Open terminal, run: docker-compose up -d
3. Open database, add 2 databases from folder database with correct name of file
4. Open terminal, run: dotnet run
